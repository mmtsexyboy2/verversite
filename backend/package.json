{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend service",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "node-pg-migrate",
    "migrate:create": "npm run migrate create --",
    "migrate:up": "npm run migrate up",
    "migrate:down": "npm run migrate down",
    "db:migrate:up": "node-pg-migrate up -m migrations -j migrations-config.js",
    "start:docker": "npm run db:migrate:up && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.5.0",
    "express": "^4.17.1",
    "express-session": "^1.18.1",
    "jsonwebtoken": "^9.0.2",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "pg-migrate": "^2.0.1"
  }
}
