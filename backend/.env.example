DATABASE_URL="postgresql://user:password@postgres:5432/verversite_db"
GOOGLE_CLIENT_ID="293698260918-ll31bqfqtjppcmkvrssr65pss9k9hqtd.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET_HERE" # Replace with your actual secret or leave blank if for example only
JWT_SECRET="YOUR_STRONG_RANDOM_JWT_SECRET_HERE" # Replace with a generated secret
PORT=3001
FRONTEND_URL="http://localhost:3000"
ADMIN_EMAILS="youradmin@example.com,anotheradmin@example.com" # Comma-separated list of admin emails

# Notes:
# - GOOGLE_CLIENT_SECRET and JWT_SECRET should be kept private and not committed to version control if they are real secrets.
# - For JWT_SECRET, generate a strong random string (e.g., openssl rand -base64 32).
# - The DATABASE_URL assumes you are running the PostgreSQL service named 'postgres' as defined in docker-compose.yml.
# - Update ADMIN_EMAILS with the actual email addresses of your administrators.
