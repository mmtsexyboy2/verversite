# Frontend Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api
NEXT_PUBLIC_GOOGLE_CLIENT_ID=293698260918-ll31bqfqtjppcmkvrssr65pss9k9hqtd.apps.googleusercontent.com
# Note: For Next.js, environment variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# Sensitive keys (like API secrets) should not be stored here or prefixed with NEXT_PUBLIC_.
# They should be used server-side (e.g., in API routes or getServerSideProps if Next.js backend).
